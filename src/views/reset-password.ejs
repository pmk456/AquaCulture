<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Set New Password - AquaCulture Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-blue-50 to-blue-100 min-h-screen flex items-center justify-center">
    <div class="bg-white rounded-lg shadow-xl p-8 w-full max-w-md">
        <div class="text-center mb-6">
            <h1 class="text-2xl font-bold text-blue-600 mb-1">Choose a new password</h1>
            <p class="text-gray-600 text-sm">
                Create a strong password that you don’t use elsewhere.
            </p>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="mb-4 rounded-md bg-red-50 border border-red-200 px-4 py-3 text-sm text-red-700">
                <%= error %>
            </div>
        <% } %>

        <% if (!token) { %>
            <div class="text-sm text-gray-600">
                This reset link is no longer valid. Please request a new password reset from the login page.
            </div>
            <div class="mt-6 text-center">
                <a href="/login" class="text-xs text-blue-600 hover:text-blue-800">Back to login</a>
            </div>
        <% } else { %>
            <form action="/reset-password/<%= token %>" method="POST" class="space-y-6">
                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-lock mr-2"></i>New password
                    </label>
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        required
                        minlength="8"
                        autocomplete="new-password"
                        class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                        placeholder="At least 8 characters"
                    >
                </div>

                <button 
                    type="submit"
                    class="w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
                >
                    <i class="fas fa-check mr-2"></i>Save new password
                </button>
            </form>

            <div class="mt-6 text-center">
                <a href="/login" class="text-xs text-gray-500 hover:text-gray-700">
                    Back to login
                </a>
            </div>
        <% } %>
    </div>
</body>
</html>


