<aside class="w-64 bg-white border-r border-gray-200 flex flex-col">
    <div class="p-6 border-b border-gray-200">
        <h1 class="text-2xl font-bold text-blue-600">B.Tech AquaCulture</h1>
        <p class="text-sm text-gray-500 mt-1">Admin Panel</p>
    </div>
    <nav class="flex-1 overflow-y-auto p-4">
        <ul class="space-y-2">
            <li>
                <a href="/dashboard" class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-blue-50 hover:text-blue-600 transition-colors <%= currentPage === 'dashboard' ? 'bg-blue-50 text-blue-600' : '' %>">
                    <i class="fas fa-chart-line w-5 mr-3"></i>
                    <span>Dashboard</span>
                </a>
            </li>
            <li>
                <a href="/users" class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-blue-50 hover:text-blue-600 transition-colors <%= currentPage === 'users' ? 'bg-blue-50 text-blue-600' : '' %>">
                    <i class="fas fa-users w-5 mr-3"></i>
                    <span>Users</span>
                </a>
            </li>
            <li>
                <a href="/dealers" class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-blue-50 hover:text-blue-600 transition-colors <%= currentPage === 'dealers' ? 'bg-blue-50 text-blue-600' : '' %>">
                    <i class="fas fa-store w-5 mr-3"></i>
                    <span>Dealers</span>
                </a>
            </li>
            <li>
                <a href="/visits" class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-blue-50 hover:text-blue-600 transition-colors <%= currentPage === 'visits' ? 'bg-blue-50 text-blue-600' : '' %>">
                    <i class="fas fa-calendar-check w-5 mr-3"></i>
                    <span>Visits</span>
                </a>
            </li>
            <li>
                <a href="/territories" class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-blue-50 hover:text-blue-600 transition-colors <%= currentPage === 'territories' ? 'bg-blue-50 text-blue-600' : '' %>">
                    <i class="fas fa-map-marked-alt w-5 mr-3"></i>
                    <span>Territories</span>
                </a>
            </li>
            <li>
                <a href="/map" class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-blue-50 hover:text-blue-600 transition-colors <%= currentPage === 'map' ? 'bg-blue-50 text-blue-600' : '' %>">
                    <i class="fas fa-map w-5 mr-3"></i>
                    <span>Live Map</span>
                </a>
            </li>
            <li>
                <a href="/audit" class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-blue-50 hover:text-blue-600 transition-colors <%= currentPage === 'audit' ? 'bg-blue-50 text-blue-600' : '' %>">
                    <i class="fas fa-history w-5 mr-3"></i>
                    <span>Audit Logs</span>
                </a>
            </li>
            <li class="pt-4 border-t border-gray-200">
                <a href="/settings" class="flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-blue-50 hover:text-blue-600 transition-colors <%= currentPage === 'settings' ? 'bg-blue-50 text-blue-600' : '' %>">
                    <i class="fas fa-cog w-5 mr-3"></i>
                    <span>Settings</span>
                </a>
            </li>
        </ul>
    </nav>
    <div class="p-4 border-t border-gray-200">
        <a href="/logout" class="flex items-center px-4 py-3 text-red-600 rounded-lg hover:bg-red-50 transition-colors">
            <i class="fas fa-sign-out-alt w-5 mr-3"></i>
            <span>Logout</span>
        </a>
    </div>
</aside>

